interface:
  display_name: "NPM 安全发布"
  short_description: "自动判定版本、提交推送，并用持久令牌免 OTP 发布"
  default_prompt: "Use $npm-release to version, commit, push, and publish the current package changes with persistent npm authentication."

policy:
  allow_implicit_invocation: false
